/**
 * Dynamically change the behaviour of an object at run time.
 * Add functionality without inheritance.
 * Object "onion": object inside an object inside an ...
 * Decorator "is a" and "has a" decorated class.
 */
package decorator;